import { defineConfig } from 'wxt';

// See https://wxt.dev/api/config.html
export default defineConfig({
  manifest: {
    update_url: 'https://maru0014.github.io/sample-self-hosted-extension/updates.xml',
  },
});
